(function (ng) {
    'use strict';
    ng.module('app', ['lazyImage'])
        .controller('mainCtrl', ['$scope', function mainCtrl($scope) {
            // TODO generate url overhere for demo...

        }]);
})(angular);